version: 2.1 
jobs:
  compile:
   docker:
      - image: ubuntu:18.04
   steps:
      - run:
          command: |
           apt-get -y update && apt-get -y upgrade && apt-get -y install bc build-essential zip curl libstdc++6 git wget llvm python gcc clang libssl-dev
           git clone --depth=1 https://github.com/Bikram557/android_kernel_xiaomi_santoni -b ten
           cd msm-4.9_santoni
           bash build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile